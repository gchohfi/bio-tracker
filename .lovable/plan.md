

## Reimplantar a Edge Function `extract-lab-results`

### O que sera feito
Reimplantar (redeploy) a edge function `extract-lab-results` sem alteracoes no codigo. Isso garante que a versao mais recente do codigo (incluindo a correcao de referencia laboratorial para marcadores percentuais) esteja ativa no backend.

### Etapa unica
- Executar o deploy da funcao `extract-lab-results` usando a ferramenta de deploy de edge functions

Nenhuma alteracao de codigo e necessaria.

